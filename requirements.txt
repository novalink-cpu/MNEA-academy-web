# EGMS - App dependencies (install before building exe)
Pillow>=9.0.0
reportlab>=3.6.0

# Web app
flask>=2.3.0
flask-cors>=4.0.0
gunicorn>=22.0.0

# Optional unified auth (/auth/* + MySQL bcrypt for /api/login) — install for full security module
python-dotenv>=1.0.0
mysql-connector-python>=8.2.0
bcrypt>=4.1.0
flask-bcrypt>=1.0.1
flask-wtf>=1.2.0
flask-mail>=0.10.0
flask-limiter>=3.5.0
email-validator>=2.1.0

# Build tool (for creating exe)
pyinstaller>=6.0.0
